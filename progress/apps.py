"""
Configuration for the progress app.
"""

from django.apps import AppConfig


class ProgressConfig(AppConfig):
    """
    Configuration for the progress application.
    """
    default_auto_field = "django.db.models.BigAutoField"
    name = "progress"
